use wgpu;
struct Context{
    surface: wgpu::Surface,
    device: wgpu::Device,

    render_pipeline: wgpu::RenderPipeline,
    compute_pipeline: wgpu::ComputePipeline,
}

impl Context{
    fn init(

    ){
        
    }
}